<%model_class = Categoria%>
<%content_for :titulo do%>
  Detalhes de 
  <%end%>
  <dl class="dl-horizontal">
          <dt><strong>Nome:</strong></dt>
      <dd><%= @categoria.nome %></dd>
          <dt><strong>Sigla:</strong></dt>
      <dd><%= @categoria.sigla %></dd>
          <dt><strong>Lei:</strong></dt>
      <dd><%= @categoria.lei %></dd>
    
  </dl>
  <%= link_to 'Voltar', categorias_path, :class => 'btn btn-default'  %>
  <%= link_to 'Editar', edit_categoria_path(@categoria), :class => 'btn btn-default' %>
  <%= link_to "Apagar",
              categoria_path(@categoria),
              :method => 'delete',
              :data => { :confirm => "Você tem certeza?" },
              :class => 'btn btn-danger' %>