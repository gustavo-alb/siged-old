  <script type="text/javascript">
   jQuery(function() {
    jQuery('.pagination a').attr('data-remote', 'true');
  });
 </script>
 <table class="table table-striped" cellspacing="0">
  <thead>
    <tr>
      <th>Matrícula</th>
      <th>Funcionário</th>
      <th>Cargo</th>
      <th title="Regência Semanal Disponível">RSD<sup>(?)</sup></th>
     <!--  <th>Ações</th>
      <th colspan=2></th> -->
    </tr>
  </thead>
  <tbody>
    <% @funcionarios.each do |f| %>
    <tr>
     <td><%= f.matricula %></td>
     <td><%= detalhes(f.pessoa) %></td>
     <td><%= cargo_resumido(f) %></td>
     <td title='<%=turmas(f)%>'><%= cor_rsd(f)%></td>
     <%if can? :especificar_lotacao,Lotacao%>
     <!-- <td><%= link_to "Devolver Funcionário", pessoa_funcionario_lotacao_devolver_funcionario_path(f.pessoa,f,@objeto.lotacoes.ativas.find_by_funcionario_id(f.id))  %></td> -->
     <% end %>
   </tr>
   <% end %>
 </tbody>
</table>
<%= will_paginate @funcionarios, :remote => true,:params=>{:aba=>'funcionarios'}%>
