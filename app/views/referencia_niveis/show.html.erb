<%model_class = Referencia_nivel%>
<%content_for :titulo do%>
  Detalhes de 
  <%end%>
  <dl class="dl-horizontal">
          <dt><strong>Nome:</strong></dt>
      <dd><%= @referencia_nivel.nome %></dd>
          <dt><strong>Codigo:</strong></dt>
      <dd><%= @referencia_nivel.codigo %></dd>
          <dt><strong>Vencimento:</strong></dt>
      <dd><%= @referencia_nivel.vencimento_id %></dd>
          <dt><strong>Entidade:</strong></dt>
      <dd><%= @referencia_nivel.entidade_id %></dd>
          <dt><strong>Jornada:</strong></dt>
      <dd><%= @referencia_nivel.jornada %></dd>
    
  </dl>
  <%= link_to 'Voltar', referencia_niveis_path, :class => 'btn btn-default'  %>
  <%= link_to 'Editar', edit_referencia_nivel_path(@referencia_nivel), :class => 'btn btn-default' %>
  <%= link_to "Apagar",
              referencia_nivel_path(@referencia_nivel),
              :method => 'delete',
              :data => { :confirm => "Você tem certeza?" },
              :class => 'btn btn-danger' %>