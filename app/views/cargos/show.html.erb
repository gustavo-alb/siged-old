<%model_class = Cargo%>
<%content_for :titulo do%>
  Detalhes de 
  <%end%>
  <dl class="dl-horizontal">
          <dt><strong>Nome:</strong></dt>
      <dd><%= @cargo.nome %></dd>
          <dt><strong>Codigo:</strong></dt>
      <dd><%= @cargo.codigo %></dd>
    
  </dl>
  <%= link_to 'Voltar', cargos_path, :class => 'btn btn-default'  %>
  <%= link_to 'Editar', edit_cargo_path(@cargo), :class => 'btn btn-default' %>
  <%= link_to "Apagar",
              cargo_path(@cargo),
              :method => 'delete',
              :data => { :confirm => "Você tem certeza?" },
              :class => 'btn btn-danger' %>